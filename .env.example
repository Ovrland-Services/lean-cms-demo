# Copy to .env (gitignored) and fill in real values.

# GitHub PAT with `write:packages` scope, used by Kamal to push the image
# to ghcr.io/mjthompsgb/lean-cms-demo on deploy.
KAMAL_REGISTRY_PASSWORD=ghp_replace_me
